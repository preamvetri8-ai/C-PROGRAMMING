#include<stdio.h>
int main()
{
	char ch1,ch2;
	   scanf("%c%c",&ch1,&ch2);
	      if(ch1==ch2){
		        	printf("is a character are equal");
		  }else              
		  printf(" is  not a characters are equal ");
		  		    return 0; 
}